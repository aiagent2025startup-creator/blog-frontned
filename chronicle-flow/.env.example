# Frontend dev environment for Vite
# Copy this file to `.env` in the `chronicle-flow/` folder and update values for local development.

# The backend API base URL (do not include trailing /api)
VITE_API_URL=https://blog-backend-e4j1.onrender.com

# Optional: set this to match your local dev node env
VITE_NODE_ENV=development

# Example: enable verbose logging in the client
# VITE_DEBUG=true
